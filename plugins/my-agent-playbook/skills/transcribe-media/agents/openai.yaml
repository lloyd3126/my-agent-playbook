interface:
  display_name: "媒體轉錄"
  short_description: "以本機 Whisper 或 OpenAI API 產生時間軸字幕"
  default_prompt: "Use $transcribe-media to transcribe this media with timestamps using the provider I authorize."
